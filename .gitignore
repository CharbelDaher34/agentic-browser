.venv
.env

# python
__pycache__/
*.pyc

# runtime artifacts (screenshots / replay trail)
artifacts/

# frontend
frontend/node_modules/
# frontend/dist is intentionally NOT ignored: the gateway serves it single-origin
# (SERVE_UI). Rebuild before deploying: cd frontend && npm run build

# uv
.python-version
computers/
.agents/
.env.*
# ...but DO track the documented template
!.env.example
node_modules/
